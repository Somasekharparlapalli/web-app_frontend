
  # web app

  This is a code bundle for web app. The original project is available at https://www.figma.com/design/MLUdSPRxC2Y0ISeAyCdsv7/web-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  